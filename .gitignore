# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Notebook exports (never commit rendered output — see CLAUDE.md > Data safety rules)
# .py notebooks store no outputs, but any export format below CAN embed patient-level rows.
notebooks/*.html
notebooks/*.ipynb
notebooks/*.json
notebooks/__marimo__/
notebooks/_marimo_assets/
notebooks/public/
notebooks/*-export/
notebooks/*.png
notebooks/*.svg
notebooks/*.pdf

# Environments
.venv/
.env
.env.*
!.env.example

# Local machine-specific configs (never commit)
configs/local.yaml

# Data provenance with real extract details (example template stays trackable)
/data/provenance.yaml

# Data: never commit CLIF/MIMIC parquet or PHI
/data/raw/
/data/interim/
/data/processed/
/data/external/
# Allow-list semantics for /data/: ignore everything except README.md and provenance example.
/data/*
!/data/README.md
!/data/provenance.yaml.example
# Per-format globs are defense-in-depth for anywhere in the repo.
*.parquet
*.duckdb
*.db
*.sqlite
*.csv.gz
*.csv

# Outputs (derived artifacts; reproduce with `make baseline` etc.)
/reports/*.html
/reports/*.json
/reports/*.csv
/reports/*.md
/models/
/checkpoints/
/runs/

# OS / IDE
.DS_Store
.vscode/
.idea/
